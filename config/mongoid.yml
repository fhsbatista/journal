development:
  clients:
    default:
      database: journal
      hosts:
        - localhost:27017
      options:
        # Adicione opções aqui, se necessário

test:
  clients:
    default:
      database: journal
      hosts:
        - localhost:27017